# RAG Spike Report (Phase 0)

Generated by the Phase 0 spike engine (`Fahad_AI_Rag_Spike`, also exposed as `wp fahad-ai rag-spike`). Dataset: **canned (offline, no key)** over a demo-modelled catalog. This is the deterministic harness validation; the keyed run on live embeddings is the final confirmation.

## Verdict

**GO (provisional)** — hybrid beats keyword-only on the canned golden set and the brute-force scan latency is acceptable at the projected catalog sizes. Re-run with an OpenAI key configured for the real-embedding confirmation before Phase 1 GA.

## Relevance (recall@3)

| Query | keyword | vector | hybrid |
|---|---|---|---|
| warm apparel | 1.00 | 1.00 | 1.00 |
| music device (semantic) | 0.00 | 1.00 | 1.00 |
| cushioned running shoes (split) | 0.50 | 0.50 | 1.00 |
| **MEAN** | **0.500** | **0.833** | **1.000** |

The gate (RAG-DESIGN.md §6.1): hybrid (RRF of keyword + vector) must be >= each leg on every query and beat keyword-only overall. The split query ('cushioned running shoes') is the proof — keyword finds the literal-token item, vector finds the semantic item, only hybrid surfaces both.

## Brute-force scan latency (512-dim float32 BLOBs; unpack + cosine per row)

| Catalog size | p50 (ms) | p95 (ms) |
|---|---|---|
| 1000 | 29.21 | 29.59 |
| 5000 | 144.66 | 145.15 |
| 20000 | 577.60 | 581.19 |

Worst-case p95 across projected sizes: **581.19 ms** (on the dev runner's hardware; commodity shared hosts are slower). Category/stock/price pre-filtering shrinks the scanned set in practice (RAG-DESIGN.md §4.4), and the MariaDB-native / external backends (Phase 3) remove brute force entirely at scale.

## Decision

The harness discriminates and hybrid wins, so the approach is sound and Phase 1 is justified. Brute-force MySQL is viable at small-to-mid catalogs; the scaling escape hatches are already designed (Phase 3). Proceed to Phase 1 (S1.1 embedding provider first), and run `wp fahad-ai rag-spike` with a key on a representative catalog to confirm relevance on real embeddings.
